<section id="party-detail">
  <h2><%= @party.name %></h2>

  <ul>
  <% @party.questions.each do |question| %>
    <li><%= link_to question.title, main_app.question_path(question) %></li>
  <% end %>
  </ul>
</section>